source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"   # needed for `jekyll serve` on Ruby ≥ 3
# jekyll-seo-tag, jekyll-sitemap, jekyll-feed are bundled via github-pages
